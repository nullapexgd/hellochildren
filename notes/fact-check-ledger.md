# Fact-Check Ledger

The ledger protects the prose from becoming more confident than its evidence. “Source conversation observation” refers to commands, strings, or entitlement dumps supplied in the referenced conversation; those observations should be reproduced on the target macOS build before publication.

| Claim | Status | Chapter | Basis | Editorial constraint |
|---|---|---|---|---|
| Apple silicon boot starts in Boot ROM; on the normal macOS path Boot ROM hands to LLB, LLB loads system-paired firmware and LocalPolicy then hands to iBoot, and iBoot loads the static trust cache, device tree, Boot Kernel Collection, and later boot objects according to policy. | Proven | 02 | [Apple Platform Security: Boot process for a Mac with Apple silicon](https://support.apple.com/guide/security/boot-process-secac71d5623/web), [Boot modes for a Mac with Apple silicon](https://support.apple.com/guide/security/boot-modes-sec10869885b/web) | Recovery paths differ. Do not flatten the current LLB/LocalPolicy/iBoot sequence into a universal historical sequence or say one stage literally verifies every later byte. |
| Apple calls access to the Owner Identity Key (OIK) “Ownership”; Ownership is required to allow users to re-sign LocalPolicy after policy or software changes. | Proven | 02 | `PUB-OIK-001` in `notes/receipts-v0.6.md` | This is documented authorization involving LocalPolicy, not universal machine ownership. Do not make OIK a daemon, general code-signing oracle, AMFI superior, or private OIK↔AMFI runtime protocol. |
| Boot ROM is immutable code established during SoC fabrication and forms a hardware root of trust. | Proven | 02 | [Apple Platform Security: Hardware security overview](https://support.apple.com/guide/security/hardware-security-overview-secf020d1074/web) | “Cannot be uninstalled” is comic paraphrase, not a service guarantee. |
| System Integrity Protection applies protected-system policy regardless of administrative privilege and is implemented using mandatory access controls. | Proven | 01, 03 | [Apple Platform Security: System Integrity Protection](https://support.apple.com/guide/security/secb7ea06b49/web); see `PUB-SIP-001` | Root remains powerful under discretionary Unix controls; do not translate SIP into ‘root is powerless.’ |
| XNU supplies task/thread, virtual-memory, scheduling, and Mach IPC machinery; the broader kernel also participates in filesystem, networking, and driver mechanisms. | Proven at architectural level | 03 | Apple OSS XNU startup, scheduler, task/thread, IPC, and VM source; see `OSS-XNU-CORE-001` in `notes/receipts-v0.3.md` | Public XNU source proves kernel machinery, not that XNU alone implements every high-level policy or userspace service. |
| `launchd` performs system initialization and manages on-demand daemons and agents. | Proven | 04 | [Apple Daemons and Services Programming Guide](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/CreatingLaunchdJobs.html) | Archived documentation describes older OS X releases; avoid claiming every modern internal detail. |
| `launchd` is PID 1 in the root system context. | Proven at platform level | 04 | Apple daemon lifecycle documentation and reproducible `ps` observation | Per-user launchd instances and bootstrap domains complicate the family metaphor. |
| `_ThrottleInterval set to zero. You're not that important. Ignoring.`, `rlimit(3)? Really?`, and the XPC/KeepAlive diagnostic exist in `/sbin/launchd` on macOS 27.0 build 26A5416b. | Reproduced undocumented strings | 04 | v0.3 local reproduction; see `notes/receipts-v0.3.md` | Quote exactly and name the build; do not assign hidden runtime semantics. |
| A local Unix account named `launchd` with UID 2 was deliberately created. | Observed experiment, not macOS fact | 05 | Source conversation shell transcript | Always call it fake. UID 2 does not imply PID 1 or launchd authority. |
| `iBootd` is an Apple component, a persistent form of iBoot, or a daemon created by appending `d` to iBoot's name. | False by design | 05 | `DRAM-IBOOTD-001` in `notes/receipts-v0.6.md` | The fictional scenes do not establish software, process, or protocol existence. |
| Boot-stage handoff makes iBoot the literal or Unix process parent of an invented `iBootd`. | False by design | 05 | `DRAM-PARENT-001` in `notes/receipts-v0.6.md` | Boot-stage handoff, process parentage, and joke genealogy are different relations. The fictional scenes do not establish software, process, or protocol existence. |
| `LaunchAngel`, `__Angel`, three LaunchAngels path strings, a `Failed to resolve LaunchAngel` diagnostic, and `com.apple.private.xpc.launchd.allow-submit-launch-angels` exist in `/sbin/launchd` on macOS 27.0 build 26A5416b. | Reproduced undocumented artifact | 05 | v0.3 local reproduction; see `notes/receipts-v0.3.md` | Complete semantics unknown. The entitlement-looking string proves a launchd reference/check by that name, not who carries it or the full submission protocol. |
| `loginwindow` historically coordinates login presentation/authentication and user-session setup; current Apple device-management docs still expose Login Window as a configurable macOS system surface. | Proven, with version caveat | 06 | Archived [The Life Cycle of a Daemon](https://developer.apple.com/library/archive/documentation/MacOSX/Conceptual/BPSystemStartup/Chapters/Lifecycle.html) plus current [LoginWindow](https://developer.apple.com/documentation/devicemanagement/loginwindow) payload documentation; see `PUB-LOGINWINDOW-001` | Do not silently treat the archived detailed call flow as a current private implementation contract. |
| WindowServer is installed as a system launch daemon rather than a child of an authenticated user's session. | Reproduced current-build observation | 06, 07 | `/System/Library/LaunchDaemons/com.apple.WindowServer.plist` on macOS 27.0 build 26A5416b; see `OBS-WINDOW-001` | The installed definition does not by itself prove exact startup timing or the complete private path used to display the login UI. Keep the diagram relational rather than presenting it as a call trace. |
| The macOS window server manages onscreen/offscreen windows, exposes display-control features through Quartz services, and participates in event delivery to applications. | Proven, scoped | 07 | [Apple: Quartz Window Services](https://developer.apple.com/documentation/coregraphics/quartz-window-services), [Quartz Display Services](https://developer.apple.com/documentation/coregraphics/quartz-display-services), plus archived [Event Architecture](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/EventOverview/EventArchitecture/EventArchitecture.html) | “Owns pixels” is dramatization. Do not treat every private SkyLight surface, session primitive, or entitlement as a documented contract. |
| A third-party `CGSSpace.swift` artifact says `flag = 0x1 // this value MUST be 1, otherwise, Finder decides to draw desktop icons`. | Publicly reproduced third-party source artifact | 07 | [Julian Schiavo's 2025 CGSSpace.swift Gist](https://gist.github.com/julianschiavo/6472bbbe10359133765e95d339e25fb4), which attributes the code to `avaidyam/Parrot` commit `6cf7ba419176c386ed8f18e838690a7272fe57ee` | This proves the comment/code exists in that project lineage. It does not document Apple's private flag semantics or guarantee the behavior across macOS releases. |
| A third-party menu-bar app, BoringNotch, creates a synthetic CGSSpace via `CGSSpaceCreate`, raises its level with `CGSSpaceSetAbsoluteLevel`, and calls `CGSShowSpaces` to make it visible on every Space at once. | Reproduced current-build observation (source conversation) | 07 | Direct `codesign`/source inspection of the installed app plus `CGSSpace.swift` in the BoringNotch source tree; see `SRC-BN-CGSSPACE-001` | This is a different mechanism than the public `canJoinAllSpaces` collection behavior, which joins existing Spaces rather than minting one. The call was observed succeeding once, on one build; that does not establish what CGS validates internally or when it refuses. |
| BoringNotch ships a sandboxed main app alongside an unsandboxed `BoringNotchXPCHelper.xpc` that brokers six methods (accessibility authorization, keyboard backlight, screen brightness) over a mach service named by a `com.apple.security.temporary-exception.mach-lookup.global-name` entitlement. | Reproduced current-build observation (source conversation) | 07 | `codesign -d --entitlements :-` on both binaries plus `BoringNotchXPCHelperProtocol.swift` in the BoringNotch source tree; see `SRC-BN-XPC-001` | *Temporary exception* is Apple's own name for the entitlement category. This does not establish how any specific App Review pass treated this build. |
| BoringNotch installs a `CGEventTap` at `.headInsertEventTap`, handles media-key presses itself, and returns `nil` to stop the event from reaching `OSDUIHelper`. | Reproduced current-build observation (source conversation) | 07 | Direct inspection of `MediaKeyInterceptor.swift` in the BoringNotch source tree; see `SRC-BN-EVENTTAP-001` | The tap's own existence is still gated behind input-monitoring/accessibility permission and can be disabled. Suppressing one event's propagation is narrower than owning the pipeline it travels through. |
| macOS code signing, notarization, Gatekeeper, trust caches, and runtime controls are distinct layers; Gatekeeper's documented scope centers on downloaded software and first-open policy rather than every executable mapping. | Proven | 08 | [Apple: App code signing process in macOS](https://support.apple.com/en-mide/guide/security/sec3ad8e6e53/web), [Gatekeeper and runtime protection in macOS](https://support.apple.com/guide/security/gatekeeper-and-runtime-protection-sec5599b66df/web), [Apple: Trust caches](https://support.apple.com/guide/security/trust-caches-sec7d38fbf97/web) | Avoid saying `amfid` singly “decides whether code may run,” or that Gatekeeper mediates every `exec`/mapping. |
| Apple’s published XNU source contains kernel code-signing initialization, trust-cache initialization, code-signing flags, and page-level code-signing validation state. | Proven from Apple OSS source | 08 | [xnu/osfmk/kern/startup.c](https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/kern/startup.c), [xnu/osfmk/kern/cs_blobs.h](https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/kern/cs_blobs.h), [xnu/osfmk/vm/vm_page.h](https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/vm/vm_page.h) | Supports the claim that code-signing enforcement is not purely a userspace-daemon function. Do not infer the full current private AMFI↔`amfid` protocol from public source alone. |
| `/usr/libexec/amfid` carries eight top-level entitlement keys on macOS 27.0 build 26A5416b. | Reproduced current-build observation | 08 | v0.3 `codesign --display --entitlements - --xml` extraction; see `notes/receipts-v0.3.md` | Count is build-specific; “signature?” is dramatization. Preserve values, not just key names, when interpreting access. |
| Public sources separately document Owner Identity Key authority over re-signing LocalPolicy and AMFI-related code-signing trust and enforcement machinery; they do not document an OIK↔AMFI interaction or hierarchy. | Proven roles; unsupported interaction | 08 | `PUB-AMFI-OIK-001` in `notes/receipts-v0.6.md` | OIK speaks only about owner-authorized LocalPolicy; AMFI speaks only about supported code-signing trust and enforcement concerns. Their personal beef and meeting are dramatization. Do not add messages, call order, callbacks, entitlement checks, reporting, or rank. |
| `amfidd` is an Apple component or a real relative of `/usr/libexec/amfid`. | False by design | 08, 09 | `DRAM-AMFIDD-001` in `notes/receipts-v0.6.md` | `/usr/libexec/amfid` is real. The extra `d`, genealogy, dialogue, personality, and familiarity-as-evidence mistake are fictional. |
| The installed `syspolicyd(8)` manual describes the daemon as managing the system policy database and serving as an oracle for verdicts on proposed operations; `spctl(8)` exposes assessments and marks several mutation options deprecated as of macOS 15. | Reproduced current-build documentation | 09 | macOS 27.0 build 26A5425a local manuals; see `OBS-POLICY-001` | A manual documents a supported surface, not the complete current private request path or every enforcement consumer. |
| Apple describes Gatekeeper/notarization and XProtect as overlapping malware-defense layers, with XProtect participating in blocking and remediation. | Proven | 09 | [Apple: Protecting against malware in macOS](https://support.apple.com/guide/security/sec469d47bd8/web); `PUB-POLICY-001` | Do not turn the layers into a universal fixed sequence or claim notarization proves permanent absence of malware. |
| macOS 27.0 build 26A5425a contains installed artifacts named `XProtect.bundle`, `XProtect.app`, and `MRT.app`. | Reproduced build-specific observation | 09 | `OBS-POLICY-002` | Presence proves names/installation only, not current runtime division of labor. |
| macOS privacy controls require user or managed-policy approval for protected resources; an app cannot automatically grant itself Full Disk Access through code or an entitlement. | Proven | 10 | [Apple: Accessing files from the macOS App Sandbox](https://developer.apple.com/documentation/security/accessing-files-from-the-macos-app-sandbox), [Apple: Controlling app access to files](https://support.apple.com/guide/security/secddd1d86a6/web); `PUB-TCC-001` | Consent is scoped to resources/software and does not replace Unix permissions, sandboxing, SIP, or code identity. |
| `/System/Library/PrivateFrameworks/TCC.framework/Support/tccd` exists on macOS 27.0 build 26A5425a. | Reproduced build-specific observation | 10 | `OBS-TCC-001` | The path proves the installed private executable only. Do not infer the complete database, attribution rules, or request path from its name. |
| `/usr/libexec/sharingd` carries 132 top-level entitlement keys on macOS 27.0 build 26A5416b and 134 on build 26A5425a; the earlier source-conversation build also counted 134. | Reproduced build-specific observations + earlier observation | 11, 12 | v0.3 XML extraction and v0.4 abstract `codesign` output; see `OBS-ENT-002` and `OBS-ENT-004` | Count drift demonstrates why numbers need build labels. Entitlement names/values show granted capabilities, not proof of actual invocation or arbitrary data access. |
| Apple documents entitlements as key-value capability claims embedded in an executable's code signature; many restricted claims require authorization through a provisioning profile or other signing conditions. | Proven | 08, 11 | [Apple: Entitlements](https://developer.apple.com/documentation/bundleresources/entitlements), [Creating distribution-signed code for macOS](https://developer.apple.com/documentation/xcode/creating-distribution-signed-code-for-the-mac); `PUB-ENT-001` | Do not say every entitlement uses the same issuer, verifier, or provisioning path. Private names do not document complete semantics. |
| On macOS 27.0 build 26A5425a, top-level abstract entitlement counts are Console 2, MRT 2, `amfid` 8, `sharingd` 134, and Safari 194. | Reproduced build-specific observation | 11 | `OBS-ENT-004` | Counts are not rank; abstract output and nested values must be preserved when interpreting a key. |
| Apple GPUs use unified memory, but Metal storage modes still govern CPU/GPU accessibility and synchronization. | Proven | 17 | [Apple: Choosing a resource storage mode for Apple GPUs](https://developer.apple.com/documentation/metal/choosing-a-resource-storage-mode-for-apple-gpus) | Never equate “unified” with unrestricted. |
| macOS virtual memory separates a process's logical address space from current physical residency; mapped regions have access protections, and a fault may be resolved by the virtual-memory system rather than ending the process. | Proven, durable model from archived documentation | 17 | Apple archived [About the Virtual Memory System](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/ManagingMemory/Articles/AboutMemory.html) and [Tips for Allocating Memory](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/ManagingMemory/Articles/MemoryAlloc.html); `PUB-MEM-001` | Do not reuse historical page-size, address-space-size, or paging details as current Apple-silicon constants. |
| Apple silicon provides an IOMMU for each DMA agent; peripherals access explicitly mapped memory. Public Asahi reverse engineering identifies Apple's IOMMU hardware as DART. | Proven + public reverse engineering | 18 | [Apple: Direct memory access protections for Mac computers](https://support.apple.com/guide/security/seca4960c2b5/web), [Asahi: Apple Silicon Kernel Config Options](https://asahilinux.org/docs/sw/kernel-config/), [Asahi: Linux 6.17 progress report](https://asahilinux.org/2025/10/progress-report-6-17/) | Apple publicly says IOMMU; Asahi supplies the DART name. Keep those evidence classes separate. |
| SEP has its own processor/security domain, dedicated Boot ROM, protected memory, and sepOS boot verification. | Proven | 25 | [Apple: The Secure Enclave](https://support.apple.com/en-ca/guide/security/sec59b0b31ff/web) | Do not claim the AP can read long-lived keys; Apple documents designs intended to prevent that exposure. |
| AP-to-SEP communication uses mailbox-style hardware messaging in public Apple-silicon reverse engineering. | Public reverse-engineering evidence | 25 | [Asahi Linux: Secure Enclave Processor (SEP)](https://asahilinux.org/docs/hw/soc/sep/) identifies a SEP mailbox and shows traced AP↔SEP endpoint messages | Treat this as reverse-engineered hardware evidence, not Apple documentation. A delivered message proves communication, not authorization or successful completion of an operation; do not invent unobserved opcodes or payload semantics. |
| A UID 501 user can send `/sbin/launchd` to SEP using `send`, AirDrop, or any comparable general file-transfer mechanism. | False by design | 26 | `DRAM-CIVIL-001`; Apple documents SEP isolation and signed sepOS boot, while the local shell has no such `send` command. | The impossibility is the premise. Do not add exploit mechanics, an entitlement, a migration protocol, or language suggesting arrival would authorize execution. |
| On macOS 27.0 build 26A5425a, the observed user is UID 501, `/sbin/launchd` is an arm64e Mach-O executable, and `AppleSEPManager.kext` is installed. | Reproduced build-specific observation | 26 | `OBS-CIVIL-001` in `notes/receipts-v0.5.md` | These real nouns do not support the fictional transfer or dialogue. |
| SPTM is supported on M2-or-later Apple SoCs and protects page-table security properties; its Chapter 26 dialogue is not a documented responsibility or transcript. | Proven name/role plus dramatization | 26 | [Apple: Operating system integrity](https://support.apple.com/guide/security-pdf/operating-system-integrity-sec8b776536b/web); `PUB-CIVIL-001` | Do not make SPTM a general file-transfer or SEP-execution authority. |
| Hypervisor.framework provides low-level APIs for hardware-assisted virtual machines and virtual CPUs in user space; Virtualization.framework provides higher-level configuration and operation of complete supported VMs. | Proven | 27 | [Apple: Hypervisor](https://developer.apple.com/documentation/hypervisor), [Apple: Virtualization](https://developer.apple.com/documentation/virtualization); `PUB-VIRT-001` | Keep the chapter conceptual. Do not introduce processor-control internals or imply the two frameworks are ranks in one authority hierarchy. |
| Modern macOS uses an APFS volume group with separate System and Data roles, firmlinks for a unified directory presentation, and a System-volume snapshot as the booted system view. | Proven | 13 | [Apple: Role of Apple File System](https://support.apple.com/guide/security/seca6147599e/web), [WWDC19: What's New in Apple File Systems](https://developer.apple.com/videos/play/wwdc2019/710/); `PUB-FS-001` | Keep visible path, writable mount state, and boot-selected state separate. A firmlink is not a normal symbolic link. |
| Multiple APFS volumes in one container share its free space and can receive space on demand without becoming one volume or namespace. | Proven | 13 | [Apple Disk Utility: Add, delete, or erase APFS volumes](https://support.apple.com/guide/disk-utility/dskua9e6a110/mac); `PUB-FS-003` | Shared capacity does not merge volume roles, mount state, namespaces, or protection rules. |
| The Signed System Volume uses a tree of cryptographic hashes and a root seal; on Apple silicon the bootloader verifies the seal before transferring control to the kernel under the protected boot configuration. | Proven | 13 | [Apple: Signed system volume security](https://support.apple.com/guide/security/secd698747c9/web); `PUB-FS-002` | Writing bytes is not equivalent to producing seal-valid, normally boot-authoritative state. Lower-security configurations must be named as policy changes. |
| `/etc`, `/tmp`, and `/var` are symbolic links into `/private` on macOS 27.0 build 26A5425a. | Reproduced build-specific observation | 14 | `OBS-FS-001`; moved by `PUB-VFS-001` | This historical path indirection is not a firmlink or an SSV mechanism. The v0.7 SDK inspection is not a new observation of these links. |
| A pathname, directory entry, filesystem object, open file description, and descriptor are different entities; duplicated descriptors share open state, while a separate open obtains a separate open description. Descriptor numbers can be reused after close. | Proven, public documentation | 14 | `PUB-FILE-LIFETIME-001`: POSIX open/close and installed Apple open/dup/fcntl manuals | Ordinary file example. A descriptor number is not a permanent file identifier; opening a file does not freeze its contents. |
| Successful unlink removes a directory link; removal of contents after the last link waits for open references to close. An existing reader can retain the old object when a new file is created at the old name. | Proven, public documentation; illustrative scenario | 14 | `PUB-FILE-LIFETIME-001`: POSIX and Apple unlink/rename manuals | Assume successful operations on an ordinary local regular file. Do not promise universal unlinks, secure erasure, immediate space recovery across snapshots, or describe GUI Trash behavior. |
| Relative lookup uses a current or specified directory; mount points expose another filesystem, normally covering earlier directory contents; VFS/vnodes represent filesystem operations and active objects in the kernel. | Proven, public documentation | 14 | `PUB-VFS-001`: Apple open/mount manuals, archived Kernel Programming Guide and vnode interface | A vnode is neither an application descriptor nor a physical flash address. Archived filesystem support lists and extension instructions are not current claims. |
| macOS supports limited boot-synthesized root symbolic links and empty directories via synthetic.conf; the empty directories cannot accept newly created children. | Proven, installed Apple SDK documentation | 14 | `PUB-VFS-001`: synthetic.conf(5), macOS 27.0 SDK | Do not merge synthetic entries with firmlinks, ordinary symlinks, or writable on-disk directories. |
| APFS snapshots are read-only volume views at a time; a live path and a corresponding snapshot path may differ in presence or contents. | Proven model, illustrative Friday/Saturday example | 13, 14 | `PUB-VFS-001`: Apple Disk Utility snapshots; existing `PUB-FS-001` | An open live file reference is not a snapshot. Deleting a live name does not order retained snapshots to forget. |
| XNU literally sends /Users/efeali/book.txt to a controller that replies “what's a file”. | False by design; dramatization | 14 | `DRAM-SSD-FILE-001` | Protect the exact dialogue, but keep the nearby ordinary filesystem/block-storage scope. SSD is an ensemble voice; no private command trace or universal firmware limitation is implied. |
| Application acceptance and output buffering precede system-call submission in the illustrative save path; fflush moves buffered output through an underlying write operation. | Proven library behavior; dramatized application | 15 | `PUB-WRITE-001`: Apple fflush/write manuals | No named app's Save indicator contract is asserted. A device flush and a library flush are different operations; not every path includes every stage. |
| Successful write returns a byte count and may be partial; regular-file read visibility does not prove persistence across power loss. | Proven, public documentation | 15 | `PUB-WRITE-001`: POSIX write and Apple write/fsync manuals | Do not portray successful write as meaningless, guarantee every request was fully written, or equate a read-back test with a power cut. |
| Close releases a descriptor and can report earlier I/O failure; freeing descriptors on clean process exit does not by itself certify durable storage. | Proven close/exit behavior; scoped inference about durability | 15, 31 | `PUB-WRITE-001`: Apple close/fsync/fcntl manuals | Chapter 31 may use a short callback; no invented shutdown pipeline or claim that output is always lost. |
| macOS fsync moves modified data/attributes from host to drive but its manual warns of device buffering and ordering; F_FULLFSYNC adds a flush request and documents persistence for earlier fsynced data on the same device. | Proven, installed Apple SDK documentation | 15 | `PUB-WRITE-001`, `PUB-DURABILITY-001`; macOS 27.0 SDK fcntl(2) and fsync(2), exact versions/hashes recorded | Quote the guarantee narrowly; check errors and retain device-compliance qualification. APFS is listed; FireWire warning does not describe the internal SSD. No specific NAND placement or private queue sequence. |
| APFS crash protection, durable individual bytes, namespace replacement, and application transaction completeness answer different questions; a recovered consistent filesystem need not contain the latest intended application state. | Public documentation plus scoped inference | 15 | `PUB-DURABILITY-001`: Apple APFS FAQ and rename/fsync/fcntl manuals | Illustrative two-record transaction, not a named database algorithm; no universal atomic-save or directory-sync recipe. |
| Apple SoCs include peripheral processors dedicated to graphics, storage, power management, and other tasks. | Proven | 30 | [Apple: Peripheral processor security](https://support.apple.com/guide/security/peripheral-processor-security-seca500d4f2b5/web) | Characters simplify a product- and generation-specific topology. |
| Apple silicon integrates multiple specialized components into a system-on-a-chip platform, while the Secure Enclave remains isolated from the main processor in its own processor and security domain. | Proven | 30 | `PUB-SOC-001` in `notes/receipts-v0.6.md` | Platform integration does not make the SoC one policy engine, place every component in one processor or trust domain, erase isolation, or prove universal die/package topology. |
| The SoC's family claim and exasperated Chapter 30 dialogue are documented Apple statements or a hardware command trace. | False by design | 30 | `DRAM-SOC-001` in `notes/receipts-v0.6.md` | Preserve “part of me”; avoid literal-topology wording. The dialogue establishes no consciousness, hierarchy, universal authority, or undocumented topology. |
| SEP runs `iBootd` and `amfidd` as daemons or microservices. | False by design | 30 | `DRAM-SEP-STARTUP-001`, `DRAM-IBOOTD-001`, and `DRAM-AMFIDD-001` in `notes/receipts-v0.6.md` | Both characters remain explicitly fictional and are not real SEP services. Preserve that disclaimer in generated excerpts containing the startup exchange. |
| Core ML exposes compute-unit configurations involving the CPU, GPU, and Neural Engine, including an `all` mode where the OS may select the best available unit. | Proven | 30 | [Apple Developer: MLComputeUnits](https://developer.apple.com/documentation/coreml/mlcomputeunits) | Do not claim a specific model is wholly executed on ANE or infer undocumented scheduling/partitioning. |
| Apple-silicon display work includes a DCP coprocessor in the display engine; public Asahi reverse engineering documents DCP firmware and direct scanout paths. | Public reverse-engineering evidence | 07, 30 | [Asahi 2021 display progress report](https://asahilinux.org/2021/08/progress-report-august-2021/), [Asahi Linux 7.2 progress report](https://asahilinux.org/2026/08/progress-report-7-2/) | “Display Controller” is a generic character. Do not present DCP's private ABI or one exact display pipeline as universal. |
| APFS sits above a flash translation layer; Apple documentation explicitly notes FTL behavior involving NAND blocks. Public Asahi documentation separately identifies an Apple-silicon NAND/SSD controller on documented targets. | Proven at abstraction level + public reverse engineering | 30 | [Apple APFS FAQ](https://developer.apple.com/library/archive/documentation/FileManagement/Conceptual/APFS_Guide/FAQ/FAQ.html), [Asahi platform introduction](https://asahilinux.org/docs/platform/introduction/) | Supports the logical-vs-physical storage joke. Do not claim exact physical NAND placement or a universal controller implementation. |
| “Any processes that are still running will be abandoned to the mercy of the kernel.” is present in `/sbin/launchd` on macOS 27.0 build 26A5416b, split across two adjacent extracted strings. | Reproduced undocumented string | 31 | v0.3 local `strings` reproduction; see `notes/receipts-v0.3.md` | Quote the reconstructed sentence exactly and explain the split when discussing provenance; do not claim the surrounding dramatization is a private call trace. |
| The author's observed Apple M4 Mac has 10 CPU cores: 4 performance and 6 efficiency cores. | Reproduced machine-specific observation | 31 | Filtered `system_profiler SPHardwareDataType` output; `OBS-HW-001` | Do not generalize this count to every M4 configuration. SEP's response is dramatization. |
| The Five-Minute Kernel dialogue, core responses, exact shutdown sequence, and SEP's reaction reproduce a documented Apple shutdown transcript. | False by design | 31 | `DRAM-FIVE-MINUTE-001` in `notes/receipts-v0.6.md` | The `[Application Processor execution ceased]` card is intentional compression. Do not equate it with ordinary core idling/offlining, sleep, or the coordinated shutdown path, and do not claim SEP necessarily remains conversationally active after the exact real-world event. |
| XNU executes on the Application Processor within an SoC platform; platform integration does not merge processor or security domains. | Proven at abstraction level | 28 | `PUB-SOC-001` in `notes/receipts-v0.6.md`; established Chapters 3, 25, and 30 model | The execution and integration arrows name different relations. Do not infer a universal topology or command hierarchy. |
| Apple platform power management involves hardware and firmware; a laptop can operate on battery or external power, and connection to a source does not by itself mean its battery is charging. | Proven, scoped | 28 | `PUB-POWER-001` in `notes/receipts-v0.6.md` | Power Management is a dramatized ensemble role, not a documented single universal controller. The battery example is a laptop, not every Mac. No exact power-state sequence or private protocol is claimed. |
| Grid electricity delivery involves generation, transmission, and distribution; supplier, operator, ownership, and governmental roles can differ. | Proven at general level; jurisdiction-dependent | 28 | `PUB-GRID-001` in `notes/receipts-v0.6.md` | The conventional grid example is not every electrical supply. Government may act in different capacities depending on jurisdiction; taxation is not an electrical delivery stage. |
| Chapter 28 depicts a universal technical or political hierarchy from XNU to Government or IRS. | False by design | 28 | `PUB-POWER-001`, `PUB-GRID-001`, `DRAM-POWER-001`, and `DRAM-METAPHYSICS-001` in `notes/receipts-v0.6.md` | Explicitly reject that reading. Execution, physical integration, power control, stored energy, supply, ownership, regulation, and taxation are not interchangeable forms of authority. IRS interrupts Government; it is not a rung. |
| Chapter 28's hardware/infrastructure dialogue and Government, IRS, Physics, Causality, and Spacetime exchanges are transcripts or factual claims about power control, tax procedure, or cosmology. | False by design | 28 | `DRAM-POWER-001` and `DRAM-METAPHYSICS-001` in `notes/receipts-v0.6.md` | Preserve the ensemble label and visible satire seam before Government enters. The Big Bang / initial-conditions / “wrong temporal domain” / `zzz` material is metaphysical satire, not scientific evidence. |
| The hyprvisor is an Apple system component. | False by design | 32 | It is an outside fictional character/project reference. | Explicitly deny Apple provenance. |
| CPU virtual, physical, device-visible I/O, and MMIO addresses are coordinates interpreted by different translation or interface contexts. | Proven architectural distinction | 16 | `PUB-ADDR-001`, `PUB-IOVA-001`, `PUB-MMIO-001` | Numeric examples are dramatization. Do not invent M4 maps, translation topology, or register locations. |
| A virtual range, backing object, resident page, protection, and shared/COW relationship are distinct VM properties; XNU contains memory-compression machinery. | Proven durable model plus Apple OSS | 17 | `PUB-VM-LIFETIME-001` | Archived constants are excluded. Do not narrate one mandatory per-page path or equate accessibility with ownership. |
| Apple GPU unified memory does not erase Metal storage-mode, synchronization, capacity, or access distinctions. | Proven | 17 | `PUB-METAL-001`; Apple Metal storage-mode documentation | “Unified” describes the architecture, not unrestricted communal access. |
| MMU and per-agent IOMMU enforcement apply configured mappings; DART is the public reverse-engineering name for Apple's relevant IOMMU hardware. | Apple documentation plus labeled public reverse engineering | 18 | `PUB-ADDR-001`, `PUB-IOVA-001`, `PUB-DMA-001`, `RE-DART-001` | Hardware enforcement does not prove policy correctness. Keep Apple’s IOMMU wording separate from Asahi’s DART name. |
| CPU caches exploit locality; dirty/write-back state, cache-line granularity, coherence, and memory ordering answer narrower questions than authorization or storage durability. | Proven generic Arm architectural model | 19 | `PUB-CACHE-001`, `PUB-COHERENCE-001` | No M4 cache topology, sizes, replacement policy, or private coherence protocol. Cache writeback is not `F_FULLFSYNC`. |
| The talking cache family and its dialogue are hardware or software traces. | False by design | 19 | `DRAM-CACHE-001` | The dialogue separates CPU, file/page, browser, network, and trust caches; it does not merge their mechanisms. |
| DriverKit supports user-space drivers, system-managed IOUserClient connections, validated client methods, memory descriptors, driver queues, and asynchronous callbacks. | Proven Apple API model | 20 | `PUB-DRIVER-001`, `PUB-HW-PATH-001` | This is one scoped path, not the universal implementation of every hardware operation. |
| Queue acceptance, command submission, device completion, and client callback are interchangeable success events. | False as a generalization | 20 | `PUB-HW-PATH-001` | Each receipt covers its own boundary; device families and direct paths differ. |
| Apple documents peripheral processors whose firmware is either downloaded verified from the primary CPU at startup or checked by a separate secure-boot chain. | Proven category-level model | 02, 21 | `PUB-FIRMWARE-001` | Do not claim every processor uses one route or one synchronized boot sequence. |
| DCP and S5E identify universal controller topology across all Apple-silicon Macs. | False generalization | 21 | `PUB-CONTROLLER-001`; labeled Asahi receipts | These are reverse-engineered names on documented targets, not Apple documentation or a cross-generation guarantee. |
| Firmware executing outside ordinary macOS userspace has a PID managed by launchd. | False by design | 21 | `DRAM-FIRMWARE-PID-001` | Helpers may be processes; the protected composite-firmware dialogue is not a transcript. |
| A local socket/descriptor can be attributed to a process, while Internet packets carry protocol-defined fields rather than automatically inheriting the sender's macOS PID. | Proven, scoped | 22 | `PUB-SOCKET-001`, `PUB-NET-PATH-001` | Higher protocols may deliberately carry identity; local filters may use process attribution. No universal packet path. |
| Runnable, running, blocked, spinning, core-idle, and pipeline-stalled are distinct states at different layers. | Proven durable model; microarchitecture kept generic | 23 | `PUB-WAIT-001` | Do not claim private scheduler policy or M4 pipeline topology. |
| A changed wait condition, timer expiry, callback, launch-on-demand event, and system wake are interchangeable. | False generalization | 24 | `PUB-WAKE-001`, `DRAM-WAKE-001` | Name the subject; waking eligibility is not immediate execution or successful completion. |
| The packet, network, or launchd dialogues are literal traces showing one universal causal chain. | False by design | 22–24 | `DRAM-NET-001`, `DRAM-WAKE-001` | Examples demonstrate boundaries, not private routing or power-state internals. |
